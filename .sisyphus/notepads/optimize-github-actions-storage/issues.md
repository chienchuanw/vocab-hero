## 2026-04-03 Task: T1 Baseline
- `gh` CLI not authenticated — cannot run `gh cache list` or `gh api`
- This limits Task 5 (branch protection check) as well
- Workaround: Use `actionlint` for workflow validation (works offline)
